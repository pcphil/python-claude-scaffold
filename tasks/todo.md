# TODO

Agent task tracking file.  Add items here as you work; cross them off when done.

## Getting Started

- [ ] Rename `src/project_name/` to your package name
- [ ] Update `pyproject.toml`: change `project_name` to your package name in
      `[tool.hatch.build.targets.wheel]` and `[tool.coverage.run]`
- [ ] Implement `src/<your_package>/` modules (see CLAUDE.md for conventions)

## Documentation

- [ ] Fill in `docs/architecture.md` with your design decisions

## CI / CD

- [ ] Add secrets (`ANTHROPIC_API_KEY`) to GitHub Actions for integration tests
- [ ] Enable `pre-commit` in CI once all hooks pass on clean codebase
